-- 배열(array) -- 

1. 배열이란 ? : 연관된 데이터를 모아서 통으로 관리하기 위해서 사용하는 타입이다.

2. 대괄호([Element, Element])안에 콤마(,)로 구분해서 나열하면 배열이 된다.

3. 배열안에 있는 값들은 index값을 가지게된다.

4. 사용법 : A[index 값]

5. 배열의 제어
  - push : 배열의 뒤쪽에 값을 추가 할 수 있다. (예 : A.push(''))
  - unshift : 배열의 앞쪽에 값을 추가 할 수 있다. (예 : A.unshift(''))
  - splice : 특정구간을 추출하거나  (예 : A.splice(index,  , 추가할element, 추가할element 등등))
  - shift : 배열의 첫 번째 원소를 제거할 수 있다.(예 : A.shift())
  - pop : 배열의 마지막 원소를 제거한다. (예 : A.pop())
  - sort : 정렬한다 (예 : A.sort())
  - reverse : 역순 정렬한다 (예 : A.reverse())
  



