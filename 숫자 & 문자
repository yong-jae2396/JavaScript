1. date type : 연산자를 사용하여 연산할 수 있다.
  - 정수
  - 실수
  - 문자 
  - typeof : 값의 데이터 형을 알려주는 기능 
  
2. 수의 연산(number)
  - Math.pow(A, B) : A의 B승
  - Math.round( _.A) : A를 반올림
  - Math.ceil( _.A) : A를 올림
  - Math.floor( _.A) : A를 내림
  - Math.sqrt(A) : A의 제곱근
  - Math.random() : 0부터 1.0 사이의 랜덤한 숫자

3. 문자의 표현(string)
  - 큰따음표나 작은따음표로 감싸져 있으면 문자라고 한다.
  - 큰따음표 시작했으면 큰따음표로 끝나야한다  작은따음표도 마찬가지
  - \n(줄바꿈) 을 사용하여 여러줄을 표시할 수 있다.
  - 이스케이프 기법 : " " 나 ' '를 문자열 안에 포함 하고 싶을때 \를 앞에 위치시켜 포함 시키는 것 

4. 문자의 연산
  -  .length 을 문자열 뒤에 붙이면 문자의 길이를 구할수 있다.
  - 문자와 문자를 더할때는 +를 가운데 집어넣어 사용한다.
  
