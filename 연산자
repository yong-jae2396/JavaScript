-- 연산자 --
  # 연산자란 값에 대해서 어떤 작업을 컴퓨터에 지시하기 위한 기호이다.
  # 예시 : A = 1 이러한 것을 연산자라고 하고 이 연산자는 대입 연산자이다.
  # undefined : 값이 정의되어 있지 않다. (의도적이지 않는 것)
  # null : 값이 없다. (의도적으로 부여 한 것)
  # nan : 비교가 불가능한 수 일때
  
-- 비교 연산자 --
  # 비교 연산자란 주어진 값들을 비교하여 블린(boolean)의 데이터 형식인 false, true를 도출한다.
  
  1. 종류
    - 동등 연산자(equal operator) : == 
      * 데이터 형식까지 비교 X
      * 문자, 숫자 1 을 true로 간주 나머지는 false
      
    - 일치 연산자(srtict operator) : === 
      * 데이터 형식까지 비교 O
      * 이거 사용 추천 
    
    
